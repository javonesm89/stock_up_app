<!DOCTYPE html>
<html>
    <head>
        <title></title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    </head>
    <body>
        <div class="text-center">
            <% if flash.has?(:error) %>
                <div class="text-center">
                    <h6><%= flash[:error] %></h6>
                    Order for:<h4><%= @investor.orders.last.order_name %></h4>for<h4><%= @investor.orders.last.order_total.round(2) %></h4> was unable to be completed.
                    <p>Please try again with less shares for your order.</p>
                </div>
            <% end %>
            <% @investor.has_enough? %>
            <button class="btn btn-dark"><a href='/orders/new'>Create order</a></button>
            <button type="button" class="btn btn-secondary"><a class="text-light" href='/account'>Account</a></button>
        </div>
    </body>
</html